package main

import (
	"fmt"
)

/*
Middleware with Handlers
- loggin, recovery, cors
*/

func main() {
	// r := mux.NewRouter()
	// r.HandleFunc("/", home)
	// loggedRouter := handlers.LoggingHandler(os.Stdout, r)
	// http.ListenAndServe(":8080", loggedRouter)
	fmt.Println("handlers")
}
